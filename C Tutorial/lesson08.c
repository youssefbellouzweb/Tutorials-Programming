#include<stdio.h>

void main() {
    for (int x = 0; x < 10 ; x++) {
        printf("x = %i\n", x);
    }

    printf("==================\n");

    for (int x = 10; x > 0 ; x--) {
        printf("x = %i\n", x);
    }
}